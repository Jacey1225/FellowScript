"use strict";
// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const EncodedTransform_1 = require("../encodedtransformworker/EncodedTransform");
const MediaMetricsEncodedTransform_1 = require("../encodedtransformworker/MediaMetricsEncodedTransform");
const IntervalScheduler_1 = __importDefault(require("../scheduler/IntervalScheduler"));
const EncodedTransformManager_1 = __importDefault(require("./EncodedTransformManager"));
/**
 * Manages metrics collection for all media types (audio/video, sender/receiver).
 * Consolidates packet-level metrics from the Web Worker.
 */
class MediaMetricsTransformManager extends EncodedTransformManager_1.default {
    constructor(worker, logger) {
        super(worker, logger);
        this.audioSendMetrics = {};
        this.audioReceiveMetrics = {};
        this.videoSendMetrics = {};
        this.videoReceiveMetrics = {};
        this.observers = new Set();
        this.metricsReportingScheduler = null;
        this.METRICS_REPORTING_INTERVAL_MS = 1000;
    }
    /**
     * Start the metrics transform manager
     */
    async start() {
        this.startMetricsReporting();
    }
    /**
     * Get the transform names that this manager handles
     */
    transformNames() {
        return [
            EncodedTransform_1.TRANSFORM_NAMES.AUDIO_SENDER,
            EncodedTransform_1.TRANSFORM_NAMES.AUDIO_RECEIVER,
            EncodedTransform_1.TRANSFORM_NAMES.VIDEO_SENDER,
            EncodedTransform_1.TRANSFORM_NAMES.VIDEO_RECEIVER,
        ];
    }
    /**
     * Handle metrics messages from the Web Worker
     * Routes messages to the appropriate metrics property
     */
    handleWorkerMessage(message) {
        var _a, _b;
        if (message.type === MediaMetricsEncodedTransform_1.MEDIA_METRICS_MESSAGE_TYPES.NEW_SSRC) {
            /* istanbul ignore next */
            const ssrcStr = (_a = message.message) === null || _a === void 0 ? void 0 : _a.ssrc;
            if (!ssrcStr) {
                this.logger.warn('Received NEW_SSRC message without ssrc, ignoring');
                return;
            }
            const ssrc = parseInt(ssrcStr, 10);
            if (isNaN(ssrc)) {
                this.logger.warn(`Received NEW_SSRC message with invalid ssrc: ${ssrcStr}, ignoring`);
                return;
            }
            let mediaType;
            let direction;
            switch (message.transformName) {
                case EncodedTransform_1.TRANSFORM_NAMES.AUDIO_SENDER:
                    mediaType = 'audio';
                    direction = 'send';
                    break;
                case EncodedTransform_1.TRANSFORM_NAMES.AUDIO_RECEIVER:
                    mediaType = 'audio';
                    direction = 'receive';
                    break;
                case EncodedTransform_1.TRANSFORM_NAMES.VIDEO_SENDER:
                    mediaType = 'video';
                    direction = 'send';
                    break;
                case EncodedTransform_1.TRANSFORM_NAMES.VIDEO_RECEIVER:
                    mediaType = 'video';
                    direction = 'receive';
                    break;
            }
            if (mediaType && direction) {
                for (const observer of this.observers) {
                    try {
                        (_b = observer.onFirstPacketReceived) === null || _b === void 0 ? void 0 : _b.call(observer, mediaType, direction, ssrc);
                    }
                    catch (e) {
                        this.logger.error(`Error notifying first packet observer: ${e}`);
                    }
                }
            }
            return;
        }
        if (message.type !== EncodedTransform_1.COMMON_MESSAGE_TYPES.METRICS) {
            return;
        }
        try {
            const metrics = JSON.parse(message.message.metrics);
            switch (message.transformName) {
                case EncodedTransform_1.TRANSFORM_NAMES.AUDIO_SENDER:
                    this.audioSendMetrics = metrics;
                    break;
                case EncodedTransform_1.TRANSFORM_NAMES.AUDIO_RECEIVER:
                    this.audioReceiveMetrics = metrics;
                    break;
                case EncodedTransform_1.TRANSFORM_NAMES.VIDEO_SENDER:
                    this.videoSendMetrics = metrics;
                    break;
                case EncodedTransform_1.TRANSFORM_NAMES.VIDEO_RECEIVER:
                    this.videoReceiveMetrics = metrics;
                    break;
            }
        }
        catch (e) {
            this.logger.warn(`Failed to handle metrics message: ${e}`);
        }
    }
    /**
     * Add an observer to receive media metrics
     */
    addObserver(observer) {
        this.observers.add(observer);
    }
    /**
     * Remove an observer
     */
    removeObserver(observer) {
        this.observers.delete(observer);
    }
    /**
     * Start periodic metrics reporting to observers
     */
    startMetricsReporting() {
        this.metricsReportingScheduler = new IntervalScheduler_1.default(this.METRICS_REPORTING_INTERVAL_MS);
        this.metricsReportingScheduler.start(() => {
            this.reportMetrics();
        });
    }
    /**
     * Report current metrics to all observers
     */
    reportMetrics() {
        var _a;
        const metrics = {
            audioSendMetrics: { ...this.audioSendMetrics },
            audioReceiveMetrics: { ...this.audioReceiveMetrics },
            videoSendMetrics: { ...this.videoSendMetrics },
            videoReceiveMetrics: { ...this.videoReceiveMetrics },
        };
        for (const observer of this.observers) {
            try {
                (_a = observer.encodedTransformMediaMetricsDidReceive) === null || _a === void 0 ? void 0 : _a.call(observer, metrics);
            }
            catch (e) {
                this.logger.error(`Error notifying media metrics observer: ${e}`);
            }
        }
    }
    /**
     * Stop metrics transform and release resources
     */
    async stop() {
        if (this.metricsReportingScheduler) {
            this.metricsReportingScheduler.stop();
            this.metricsReportingScheduler = null;
        }
        this.observers.clear();
        // Reset all metrics to initial state
        this.audioSendMetrics = {};
        this.audioReceiveMetrics = {};
        this.videoSendMetrics = {};
        this.videoReceiveMetrics = {};
    }
}
exports.default = MediaMetricsTransformManager;
//# sourceMappingURL=MediaMetricsEncodedTransformManager.js.map