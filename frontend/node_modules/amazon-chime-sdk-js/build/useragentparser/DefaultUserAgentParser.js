"use strict";
// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
// Use "ua-parser-js" over "detect-browser" to get more detailed information.
const ua_parser_js_1 = require("ua-parser-js");
const Versioning_1 = __importDefault(require("../versioning/Versioning"));
const UserAgentParser_1 = require("./UserAgentParser");
/**
 * [[DefaultUserAgentParser]] uses UAParser to parse the browser's user agent.
 * It is responsible to hold and provide browser, OS and device specific information.
 */
class DefaultUserAgentParser {
    constructor(logger) {
        var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p;
        this.logger = logger;
        try {
            this.parserResult =
                navigator && navigator.userAgent
                    ? new ua_parser_js_1.UAParser(navigator.userAgent).getResult()
                    : undefined;
        }
        catch (error) {
            /* istanbul ignore next */
            logger.error(error.message);
        }
        this.browserMajorVersion =
            ((_c = (_b = (_a = this.parserResult) === null || _a === void 0 ? void 0 : _a.browser) === null || _b === void 0 ? void 0 : _b.version) === null || _c === void 0 ? void 0 : _c.split('.')[0]) || UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
        this.browserName = ((_d = this.parserResult) === null || _d === void 0 ? void 0 : _d.browser.name) || UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
        this.browserVersion = ((_e = this.parserResult) === null || _e === void 0 ? void 0 : _e.browser.version) || UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
        this.deviceName =
            [((_f = this.parserResult) === null || _f === void 0 ? void 0 : _f.device.vendor) || '', ((_g = this.parserResult) === null || _g === void 0 ? void 0 : _g.device.model) || '']
                .join(' ')
                .trim() || UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
        this.osName = ((_h = this.parserResult) === null || _h === void 0 ? void 0 : _h.os.name) || UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
        this.osVersion = ((_j = this.parserResult) === null || _j === void 0 ? void 0 : _j.os.version) || UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
        this.engineName = ((_l = (_k = this.parserResult) === null || _k === void 0 ? void 0 : _k.engine) === null || _l === void 0 ? void 0 : _l.name) || '';
        this.engineMajorVersion = parseInt(((_p = (_o = (_m = this.parserResult) === null || _m === void 0 ? void 0 : _m.engine) === null || _o === void 0 ? void 0 : _o.version) === null || _p === void 0 ? void 0 : _p.split('.')[0]) || '0');
    }
    getParserResult() {
        return {
            browserMajorVersion: this.browserMajorVersion,
            browserName: this.browserName,
            browserVersion: this.browserVersion,
            deviceName: this.deviceName,
            osName: this.osName,
            osVersion: this.osVersion,
            sdkVersion: Versioning_1.default.sdkVersion,
            sdkName: Versioning_1.default.sdkName,
            engineName: this.engineName,
            engineMajorVersion: String(this.engineMajorVersion),
        };
    }
    getEngineName() {
        return this.engineName;
    }
    getEngineMajorVersion() {
        return this.engineMajorVersion;
    }
    async updateWithHighEntropyValues(alwaysOverride = false) {
        var _a, _b, _c, _d;
        /* istanbul ignore next */
        // @ts-ignore - navigator.userAgentData is not yet in TypeScript's lib.dom.d.ts
        if (!((_a = navigator === null || navigator === void 0 ? void 0 : navigator.userAgentData) === null || _a === void 0 ? void 0 : _a.getHighEntropyValues)) {
            (_b = this.logger) === null || _b === void 0 ? void 0 : _b.debug('User-Agent Client Hints API not available');
            return;
        }
        try {
            // @ts-ignore - navigator.userAgentData is not yet in TypeScript's lib.dom.d.ts
            const hints = await navigator.userAgentData.getHighEntropyValues([
                'fullVersionList',
                'model',
                'platform',
                'platformVersion',
            ]);
            const shouldUpdate = (field) => alwaysOverride || field === UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
            if (hints.platform && shouldUpdate(this.osName)) {
                this.osName = hints.platform;
            }
            if (hints.platformVersion && shouldUpdate(this.osVersion)) {
                this.osVersion = hints.platformVersion;
            }
            if (hints.model !== undefined && shouldUpdate(this.deviceName)) {
                this.deviceName = hints.model || UserAgentParser_1.USER_AGENT_PARSER_UNAVAILABLE;
            }
            if ((_c = hints.fullVersionList) === null || _c === void 0 ? void 0 : _c.length) {
                const browser = this.selectPrimaryBrowser(hints.fullVersionList);
                if (browser) {
                    if (shouldUpdate(this.browserName)) {
                        this.browserName = browser.brand;
                    }
                    this.browserVersion = browser.version;
                    this.browserMajorVersion = browser.version.split('.')[0];
                }
            }
        }
        catch (error) {
            (_d = this.logger) === null || _d === void 0 ? void 0 : _d.error(`Failed to get high entropy values: ${error}`);
        }
    }
    /**
     * Selects the primary browser from a list of brand/version pairs.
     * Filters out placeholder brands (starting with "Not") and prefers
     * specific browser brands over generic "Chromium".
     */
    selectPrimaryBrowser(brands) {
        // Filter out placeholder brands (start with "Not")
        const validBrands = brands.filter(b => !b.brand.startsWith('Not'));
        // Prefer specific browsers over Chromium
        const preferredBrands = ['Google Chrome', 'Microsoft Edge', 'Opera', 'Brave', 'Vivaldi'];
        for (const preferred of preferredBrands) {
            const found = validBrands.find(b => b.brand === preferred);
            if (found)
                return found;
        }
        // Fall back to first non-Chromium brand, or Chromium if that's all we have
        return validBrands.find(b => b.brand !== 'Chromium') || validBrands[0] || null;
    }
}
exports.default = DefaultUserAgentParser;
//# sourceMappingURL=DefaultUserAgentParser.js.map