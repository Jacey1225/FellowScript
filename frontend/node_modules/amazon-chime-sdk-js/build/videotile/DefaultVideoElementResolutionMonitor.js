"use strict";
// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const VideoElementFrameMonitor_1 = __importDefault(require("./VideoElementFrameMonitor"));
class DefaultVideoElementResolutionMonitor {
    constructor() {
        this.observerQueue = new Set();
        this.element = null;
        this.frameMonitor = new VideoElementFrameMonitor_1.default();
        this.resizeObserver = new ResizeObserver(entries => {
            for (const entry of entries) {
                const { width, height } = entry.contentRect;
                for (const observer of this.observerQueue) {
                    observer.videoElementResolutionChanged(width, height);
                }
            }
        });
    }
    registerObserver(observer) {
        this.observerQueue.add(observer);
    }
    removeObserver(observer) {
        this.observerQueue.delete(observer);
    }
    bindVideoElement(newElement) {
        if (this.element === newElement) {
            return;
        }
        if (this.element) {
            this.resizeObserver.unobserve(this.element);
            this.frameMonitor.stop();
        }
        this.element = newElement;
        if (this.element) {
            this.resizeObserver.observe(this.element);
            this.frameMonitor.start(this.element, {
                firstVideoElementFrameDidRender: (metadata) => {
                    var _a;
                    for (const observer of this.observerQueue) {
                        (_a = observer.videoElementFirstFrameDidRender) === null || _a === void 0 ? void 0 : _a.call(observer, metadata);
                    }
                },
                videoElementFrameMetricsDidReceive: (metrics) => {
                    var _a;
                    for (const observer of this.observerQueue) {
                        (_a = observer.videoElementMetricsDidReceive) === null || _a === void 0 ? void 0 : _a.call(observer, metrics);
                    }
                },
            });
        }
    }
}
exports.default = DefaultVideoElementResolutionMonitor;
//# sourceMappingURL=DefaultVideoElementResolutionMonitor.js.map